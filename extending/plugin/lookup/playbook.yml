---
- hosts: localhost
  vars:
    book: "{{ lookup('shelvefile', 'file=book.db key=current_book') }}"
    page: "{{ lookup('shelvefile', 'file=book.db key=current_book_page') | int }}"

  tasks:
    - name: Print my current reading
      debug: msg="I'm on page {{ page }} of '{{ book }}'"
